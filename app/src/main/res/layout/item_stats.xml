<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/component_sizeable"
    android:layout_marginVertical="@dimen/component_extra_small">

    <TextView
        android:id="@+id/tv_item_detail_stat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ATK"
        android:textAllCaps="true"
        android:textSize="@dimen/component_sizeable"
        android:textStyle="bold"/>

    <ProgressBar
        android:id="@+id/progress_bar_item_detail_stat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:max="100"
        android:progressDrawable="@drawable/progress"/>

</LinearLayout>